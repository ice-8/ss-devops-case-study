apiVersion: v1
kind: Service
metadata:
  name: {{ include "spidersilk-app.fullname" . }}
  labels:
    {{- include "spidersilk-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spidersilk-app.selectorLabels" . | nindent 4 }}
