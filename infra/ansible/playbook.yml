---
# See infra/ansible/README.md for usage.
- name: Render and (optionally) apply SpiderSilk app configuration
  hosts: all
  gather_facts: false
  vars:
    env_name: "{{ group_names[0] }}"
  roles:
    - app_config
